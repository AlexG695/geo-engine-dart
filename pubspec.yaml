name: geo_engine_sdk
description: SDK oficial para Geo-Engine. Rastreo de ubicación y gestión de geocercas.
version: 1.0.3
homepage: https://app.geoengine.dev

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0