name: geo_engine_sdk_example
description: Demonstrates how to use the geo_engine_sdk plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  

  geo_engine_sdk:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter